node_modules
.next
.env
.env.local
.DS_Store
*.log
tsconfig.tsbuildinfo
next-env.d.ts
.claude/

# Worker: generated by `wrangler types`, and wrangler's local state
worker/worker-configuration.d.ts
.wrangler/

# Wrangler reads local secrets from here — the Slack webhook lives in it.
.dev.vars
.dev.vars.*

# Agent tooling, not application code. Installed per-machine by
# `npx skills add`, so it belongs on disk but not in this repo.
.agents/
skills-lock.json

# Output of scripts/shoot.mjs and scripts/contrast.mjs. Review artefacts from a
# local Chrome run, regenerated on demand - not something the repo should carry.
screenshots/

# Local review harness for the admin desk: hardcoded sample rows, 404s outside
# development. Useful on disk, never in the repo.
src/app/x-admin-preview/
